# Version

v0.1.0 Foundation

## Date

2026-07-21

## Completed

- Infrastructure — monorepo structure (`backend/`, `frontend/`, `infrastructure/`, `docs/`, `.github/workflows/`, `scripts/`) per SYSTEM_ARCHITECTURE.md Section 14
- Docker — multi-stage `Dockerfile`s (dev/build/production) for backend and frontend
- PostgreSQL — containerized, healthcheck-gated
- Redis — containerized, healthcheck-gated
- Prisma — first migration (`Role`, `Permission`, `RolePermission`, `Plan`), required seed script
- Angular — Angular 20, Tailwind CSS, `core`/`shared`/`layouts`/`features` structure, `ApiClient`, request-ID interceptor
- NestJS — env validation, `PrismaService`/`RedisService`, structured logging, global exception filter/response envelope/validation pipe
- Swagger — UI at `/api/docs`
- Health Checks — `/health` (liveness), `/health/ready` (Postgres + Redis)
- Nginx reverse proxy, GitHub Actions CI (install/lint/test/build)
- Husky + lint-staged + commitlint (pre-commit/commit-msg hooks)
- `scripts/setup/local-bootstrap.sh`

## Verified

- Docker Compose — `docker compose up -d --build` → all 5 services `healthy`
- Fresh Clone — full setup (env, install, migrate, seed) re-run from a genuinely clean clone simulation, not just the working dev environment
- Prisma Migration — `migrate deploy` applies cleanly against an empty database; seed populates correctly
- CI — backend and frontend lint/test/build all pass; `docker compose config` and both `Dockerfile`s validated

## Known Limitations

- No automated tests beyond scaffolding — no business logic exists yet to test
- Prisma schema has 4 of ~55 documented models — the rest arrive incrementally, one migration per milestone
- No LICENSE file yet (pending a business decision)
- WhatsApp Business Account / Meta App verification not yet submitted — external business action, not engineering; has its own lead time (IMPLEMENTATION_ROADMAP.md Section 1.3), start before Milestone 6 needs it

Two bugs caught and fixed during this release's pre-commit review — full rationale in [docs/DECISIONS.md](../DECISIONS.md) (ADR-001):
- `angular-eslint` was pinned to a version built for Angular 21 (works under `npm install`, breaks under `npm ci` — i.e. breaks Docker/CI); pinned to `20.7.0`
- Frontend had no Prettier integration and neither app had the pre-commit hook IMPLEMENTATION_ROADMAP.md Section 13 requires; added

## Next Version

v0.2.0 Authentication
