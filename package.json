{
  "name": "kapis-receptionist",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root — dev tooling only (git hooks, commit linting). See backend/ and frontend/ for the actual applications.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.3.0"
  }
}
